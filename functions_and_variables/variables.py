# Variables the way we got in programming to keep some data stored, as long as the
# is running. They can store text(string), numbers(integer or float), true or false
# (bool)
boolVariable:bool = True 
integerVariable:int = 10
floatVariable:float = 10.2


print(f"Bool = {boolVariable}\nIntegervariable = {integerVariable}\nFloatVariable = {floatVariable}")
